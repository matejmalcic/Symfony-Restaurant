{% extends 'base.html.twig' %}

{% block title %}Status index{% endblock %}

{% block body %}
    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Number</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for status in statuses %}
            <tr>
                <td>{{ status.name }}</td>
                <td>{{ status.number }}</td>
                <td>
                    <a class="btn" style="display: inline" href="{{ path('status_edit', {'id': status.id}) }}">Edit</a>
                    {{ include('crud/status/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">No records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn" href="{{ path('status_new') }}">Create new</a>
{% endblock %}
